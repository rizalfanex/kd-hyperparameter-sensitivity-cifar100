# Hyperparameter Sensitivity of Vanilla Knowledge Distillation for Compact CNNs on CIFAR-100

This repository contains the code, figures, checkpoints (except large .pt files), and scripts used for the study "Hyperparameter Sensitivity of Vanilla Knowledge Distillation for Compact CNNs on CIFAR-100". The study systematically evaluates how two core KD hyperparameters — temperature (T) and loss balancing (α) — affect compact student models (MobileNetV2 and ShuffleNetV2 ×1.0) when distilled from a ResNet-50 teacher on CIFAR-100.

---

## Abstract

Knowledge distillation has become an effective strategy for improving compact convolutional neural networks, yet the performance of vanilla knowledge distillation in lightweight image classification is still often reported using default hyperparameter settings without systematic justification. This study addresses the limited empirical understanding of how two core vanilla knowledge distillation hyperparameters, temperature scaling (T) and loss balancing (α), affect compact convolutional neural networks under a unified experimental setting. Using CIFAR-100 as the benchmark dataset, a ResNet-50 teacher was employed to distill knowledge into two lightweight student models, MobileNetV2 and ShuffleNetV2 ×1.0. Performance was evaluated using top-1 accuracy, top-5 accuracy, parameter count, and inference latency. The teacher achieved 81.24% top-1 accuracy and 96.05% top-5 accuracy. Under the default distillation setting, MobileNetV2 improved from 79.18% to 80.83% top-1 accuracy and from 95.77% to 96.40% top-5 accuracy, while reducing latency from 3.98 ms to 3.44 ms. ShuffleNetV2 ×1.0 improved from 77.00% to 78.36% top-1 accuracy and from 94.81% to 95.45% top-5 accuracy, with only a marginal latency increase from 4.23 ms to 4.29 ms. To examine hyperparameter sensitivity, an ablation study was conducted on MobileNetV2 with T = {2, 4, 6} and α = {0.3, 0.5, 0.7}. The best configuration was obtained at T = 4 and α = 0.3, yielding 80.88% top-1 accuracy and 96.51% top-5 accuracy. These results show that vanilla knowledge distillation consistently improves compact convolutional neural networks, but its effectiveness depends strongly on careful hyperparameter selection rather than inherited default settings.

---

## Highlights

- Teacher: ResNet-50 — **81.24%** top-1, **96.05%** top-5
- MobileNetV2 (baseline → KD): **79.18% → 80.83%** top-1; **95.77% → 96.40%** top-5; latency **3.98 ms → 3.44 ms**
- ShuffleNetV2 ×1.0 (baseline → KD): **77.00% → 78.36%** top-1; **94.81% → 95.45%** top-5; latency **4.23 ms → 4.29 ms**
- Ablation best (MobileNetV2): **T = 4, α = 0.3** → **80.88%** top-1, **96.51%** top-5

---

## Repo structure (important files)

- [train_kd_cifar100_paper_ready.py](train_kd_cifar100_paper_ready.py) — main training + ablation script
- [JOURNAL_PITCH.md](JOURNAL_PITCH.md) — full journal-style pitch and numbers
- [outputs_kd_paper_ready/results_summary.json](outputs_kd_paper_ready/results_summary.json) — main numerical results
- [outputs_kd_paper_ready/ablation_results.json](outputs_kd_paper_ready/ablation_results.json) — ablation runs
- [outputs_kd_paper_ready/figures/](outputs_kd_paper_ready/figures/) — publication-ready figures (600 DPI)

---

## Quick start (recommended)

1. Create a Python environment (conda recommended):

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
```

2. Install Python deps (CPU fallback if you don't have CUDA):

```powershell
python -m pip install -r requirements.txt
# Install matching torch/cu build separately as needed, for example (Windows + cu130):
python -m pip install torch==2.11.0+cu130 torchvision==0.26.0+cu130 -f https://download.pytorch.org/whl/cu130/torch_stable.html
```

3. Run the main training pipeline (this repo's default settings train teacher & students):

```powershell
python train_kd_cifar100_paper_ready.py
```

4. Run ablation only (skip full retrain, use existing teacher checkpoint):

```powershell
python train_kd_cifar100_paper_ready.py --ablation-only
```

Outputs will be written to `outputs_kd_paper_ready/` (figures, checkpoints, logs).

---

## Reproduce figures

All figures were generated by helper functions inside the main script and saved to `outputs_kd_paper_ready/figures/`. To regenerate the figures after running experiments, run:

```powershell
python train_kd_cifar100_paper_ready.py --plot-only
```

(If `--plot-only` is not present, the script supports saving figures after training; check the header of `train_kd_cifar100_paper_ready.py` for available CLI flags.)

---

## Requirements

See `requirements.txt` for the dependency list. Note: install the correct `torch` + `torchvision` wheel for your CUDA version following https://pytorch.org/get-started/locally/.

---

## How to cite

If you use this code or data in your work, please cite the project. A suggested BibTeX entry is provided in `CITATION.bib`.

---

## License

This repository is released under the MIT License. See `LICENSE` for details.

---

## Contact

If you have questions or want to reproduce the experiments on other datasets, open an issue or contact the repository owner.
